{
  "name": "typespy",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "license": "MIT",
  "bin": "dist/cli/cli.js",
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "build": "rm -rf dist || true && tsc && chmod +x dist/cli/cli.js",
    "prepare": "npm run build",
    "prepublish": "npm run test && npm run format && git diff --no-ext-diff --exit-code",
    "test": "npm run build && cd src/babel && babel tests/in.ts --out-file tests/out.ts ",
    "start": "npm run build && node dist/cli/cli.js",
    "format": "prettier --ignore-unknown --write src"
  },
  "author": "Ahmed Hassanein",
  "files": [
    "dist"
  ],
  "dependencies": {
    "@babel/cli": "^7.18.10",
    "@babel/core": "^7.18.10",
    "body-parser": "^1.20.0",
    "express": "^4.18.1",
    "ink": "^3.2.0",
    "ink-big-text": "^1.2.0",
    "ink-gradient": "^2.0.0",
    "node-notifier": "^10.0.1",
    "react": "^17.0.2"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "ts-node": "^10.9.1",
    "@types/babel__core": "^7.1.19",
    "@types/babel__template": "^7.4.1",
    "@types/express": "^4.17.13",
    "@types/node-notifier": "^8.0.2",
    "@types/react": "^18.0.15",
    "nodemon": "^2.0.19",
    "typescript": "^4.7.4"
  }
}
